extends ../layouts/master.pug

block header

block content 
  .container 
    .pages 
      ul.pages__list 
        each item in data.routing
          li.pages__item 
            +button(tag = 'a' href = item.href)
              |#{item.page}

block footer
  
