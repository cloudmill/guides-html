extends ../layouts/master.pug

include  ../components/navigation
include  ../components/thumb-slider
include  ../components/excursion-individual

block content 
  -
    var ddata = [
      {
        label: 'country',
        text: 'Russia'
      },
      {
        label: 'city',
        text: 'Saint Petersburg'
      },
    ]
  .excursion-page
    section.section.section--first.section--desktop-visible
      .container 
        .section__row 
          .section__col
            .excursion-page__breadcrumbs 
              .breadcrumb(data-aos='fade-up')
                ul.breadcrumb__list 
                  li.breadcrumb__item 
                    a.breadcrumb__link(href="#") 
                      |Home
                  li.breadcrumb__item 
                    a.breadcrumb__link(href="#") 
                      |For hotel guests
                  li.breadcrumb__item.breadcrumb__item--active 
                    a.breadcrumb__link(href="#")
                      |Folk show
            .excursion-page__thumb-slider(data-aos='fade-up')
              +thumb-slider
            .excursion-page__mobile  
              h1(data-aos='fade-up')
                |Folk show
              .sticky
                .sticky__inner.sticky__inner--small 
                  .sticky__grid.sticky__grid--area-auto
                    each item in ddata
                      .sticky__item(data-aos='fade-up')
                        .sticky__label 
                          |#{item.label}
                        .sticky__text 
                          |#{item.text}
                    .sticky__item(data-aos='fade-up') 
                      .sticky__label 
                        |tour duration
                      .sticky__text 
                        |3 hours
                .sticky__inner(data-aos='fade-up') 
                  .sticky__price 
                    |#[span.sticky__price-price $129] / person
            section.section 
              +about
          .section__col
            .sticky
              +navigation(items=['For hotel guests'] page="Folk show")
              .sticky__inner 
                .sticky__grid
                  each item in ddata
                    .sticky__item(data-aos='fade-up') 
                      .sticky__label 
                        |#{item.label}
                      .sticky__text 
                        |#{item.text}
              .sticky__inner 
                .sticky__grid
                  .sticky__item(data-aos='fade-up') 
                    .sticky__label 
                      |tour duration
                    .sticky__text 
                      |3 hours
              .sticky__inner(data-aos='fade-up') 
                .sticky__price 
                  |from #[span.sticky__price-discont $159] #[span.sticky__price-price $129] / person
    section.section 
      .container 
        +calculator-alt.calculator--noprice