mixin tour 
  .tour 
    .tour__container 
      if (attributes.title)
        h3.tour__title 
          |#{attributes.title}
      else 
        h3.tour__title 
          |Tours
      each item in attributes.text
        p.tour__text 
          |#{item.text}